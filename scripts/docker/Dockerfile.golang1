FROM alpine:latest

WORKDIR /data

COPY controller /data
COPY config.json /data
COPY goclient /data

# TODO this can be replaced with an env.list file (set by benchmark.sh) and re-use Dockerfile.golang
ENV GOMAXPROCS=1

CMD ["./controller", "--logtostderr", "-v=1", "--client=golang", "--config=./config.json"]
